apiVersion: apps/v1
kind: Deployment
metadata:
  name: employee-directory
spec:
  replicas: 1
  selector:
    matchLabels:
      app: employee-directory
  template:
    metadata:
      labels:
        app: employee-directory
    spec:
      containers:
      - name: main
        image: nginx:latest
        securityContext:
          privileged: true # This is a major security risk!
          runAsUser: 0     # Running as root is a big no-no
