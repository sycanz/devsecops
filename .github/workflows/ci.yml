name: DevSecOps Pipeline

on:
  push:
    branches:
      - main
      - stg
  pull_request:
    branches:
      - main
      - stg

permissions:
  contents: read
  id-token: write

jobs:
  stage-1-gitleaks:
    name: Stage 1 - Gitleaks (Suspender)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  stage-2a-static-analysis-sast:
    name: Stage 2a - SAST (Semgrep)
    needs: stage-1-gitleaks
    runs-on: ubuntu-latest
    if: (github.actor != 'dependabot[bot]')
    steps:
      - name: Checkout Git Repository
      - uses: actions/checkout@v6

      - name: Run Semgrep (Go + OWASP)
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/src" \
            -w /src \
            -e SEMGREP_APP_TOKEN=${{ secrets.SEMGREP_APP_TOKEN }} \
            semgrep/semgrep \
            semgrep ci --json > semgrep-results.json

      - name: Connect to Tailscale
        if: always()
        uses: tailscale/github-action@v4
        with:
          oauth-client-id: ${{ secrets.TS_OIDC_CLIENT_ID }} # using OIDC
          audience: ${{ secrets.TS_OIDC_AUDIENCE }}
          tags: tag:ci

      - name: Ingest results to DefectDojo
        if: always()
        run: |
          curl -X POST ${{ secrets.DEFECTDOJO_URL }} \
            -H "Authorization: Token ${{ secrets.DEFECTDOJO_API_KEY }}" \
            -F "scan_type=Semgrep JSON Report" \
            -F "file=@semgrep-results.json" \
            -F "engagement=1" \
            -F "minimum_severity=Low"

      - name: Notify Slack on Failure
        if: failure()
        env:
          SLACK_WEBHOOK: ${{ secrets.ONCALL_WEBHOOK_URL }}
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data '{"text":"🚨 *Security Alert:* Critical/High vulnerabilities found in Stage 2a - SAST (Semgrep) for project ${{ github.repository }}. \nCheck logs: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"}' \
          $SLACK_WEBHOOK

  stage-2b-static-analysis-sca:
    name: Stage 2b - SCA (Trivy)
    needs: stage-1-gitleaks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Git Repository
        uses: actions/checkout@v6

      - name: Run Trivy scan (Filesystem)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          hide-progress: false
          scan-type: 'fs'
          scan-ref: '.'
          scanners: 'vuln,secret'
          cache: 'false'
          format: 'json'
          output: 'trivy-fs-results.json'
          exit-code: '1'
          # severity: 'CRITICAL,HIGH'

      - name: Run Trivy scan (IaC)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          hide-progress: false
          scan-type: 'config'
          scan-ref: '.'
          scanners: 'misconfig'
          cache: 'false'
          format: 'json'
          output: 'trivy-iac-results.json'
          exit-code: '1'
          # severity: 'CRITICAL,HIGH'

      - name: Upload Scan Results
        uses: actions/upload-artifact@v4
        with:
          name: trivy-results
          path: trivy-*.json
          retention-days: 1

      - name: Connect to Tailscale
        if: always()
        uses: tailscale/github-action@v4
        with:
          oauth-client-id: ${{ secrets.TS_OIDC_CLIENT_ID }} # using OIDC
          audience: ${{ secrets.TS_OIDC_AUDIENCE }}
          tags: tag:ci

      - name: Ingest Trivy FS result to DefectDojo
        if: always()
        run: |
          curl -X POST ${{ secrets.DEFECTDOJO_URL }} \
            -H "Authorization: Token ${{ secrets.DEFECTDOJO_API_KEY }}" \
            -F "scan_type=Trivy Scan" \
            -F "file=@trivy-fs-results.json" \
            -F "engagement=1"

      - name: Ingest Trivy IaC result to DefectDojo
        if: always()
        run: |
          curl -X POST ${{ secrets.DEFECTDOJO_URL }} \
            -H "Authorization: Token ${{ secrets.DEFECTDOJO_API_KEY }}" \
            -F "scan_type=Trivy Scan" \
            -F "file=@trivy-iac-results.json" \
            -F "engagement=1"

      - name: Notify Slack on Failure
        if: failure()
        env:
          SLACK_WEBHOOK: ${{ secrets.ONCALL_WEBHOOK_URL }}
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data '{"text":"🚨 *Security Alert:* Critical/High vulnerabilities found in Stage 2b - SCA (Trivy) for project ${{ github.repository }}. \nCheck logs: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"}' \
          $SLACK_WEBHOOK

  # # Stage 3: Container Build – If Stage 2 passes, then build and push to ECR.
  # stage-3-container-build:
  #   name: Stage 3 - Build & Push to ECR
  #   needs: stage-2-static-analysis
  #   runs-on: ubuntu-latest
  #   permissions:
  #     id-token: write # Required for OIDC
  #     contents: read
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Configure AWS Credentials
  #       uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
  #         aws-region: ap-southeast-1

  #     - name: Login to Amazon ECR
  #       id: login-ecr
  #       uses: aws-actions/amazon-ecr-login@v2

  #     - name: Build, Tag, and Push Image
  #       env:
  #         ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
  #         ECR_REPOSITORY: devsecops-app
  #         IMAGE_TAG: ${{ github.sha }}
  #       run: |
  #         docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
  #         docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
  #         docker save $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG -o devsecops-app.tar

  #     - name: Upload Image Artifact
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: container-image
  #         path: devsecops-app.tar
  #         retention-days: 1

  # # Stage 4: Image Scan (Deep) – Scan the final built container image using trivy image.
  # stage-4-image-scan:
  #   name: Stage 4 - Trivy Image Scan
  #   needs: stage-3-container-build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Download Image Artifact
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: container-image

  #     - name: Load Docker image
  #       run: docker load -i devsecops-app.tar

  #     - name: Run Trivy Image scan
  #       uses: aquasecurity/trivy-action@v0.36.0
  #       with:
  #         image-ref: 'devsecops-app:${{ github.sha }}'
  #         format: 'table'
  #         exit-code: '1'
  #         severity: 'CRITICAL,HIGH'

  #     - name: Notify Slack on Failure
  #       if: failure()
  #       env:
  #         SLACK_WEBHOOK: ${{ secrets.ONCALL_WEBHOOK_URL }}
  #       run: |
  #         curl -X POST -H 'Content-type: application/json' \
  #         --data '{\"text\":\"🚨 *Security Alert:* Critical/High vulnerabilities found in Stage 4 (Image Scan) for project ${{ github.repository }}. \nCheck logs: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\"}' \
  #         $SLACK_WEBHOOK

  # # Stage 5: Deploy to EKS (Final Step)
  # stage-5-deploy:
  #   name: Stage 5 - Deploy to EKS
  #   needs: stage-4-image-scan
  #   runs-on: ubuntu-latest
  #   permissions:
  #     id-token: write
  #     contents: read
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Configure AWS Credentials
  #       uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
  #         aws-region: ap-southeast-1

  #     - name: Update Kubeconfig
  #       run: aws eks update-kubeconfig --name devsecops-cluster --region ap-southeast-1

  #     - name: Deploy to EKS
  #       run: |
  #         # Use envsubst to inject the new image tag into the k8s manifest
  #         # (Assume we have an app-deployment.yaml in k8s/)
  #         export IMAGE_TAG=${{ github.sha }}
  #         export ECR_REPOSITORY=${{ secrets.ECR_REPOSITORY }}
  #         sed -i "s|image: .*|image: $ECR_REPOSITORY:$IMAGE_TAG|g" k8s/deployment.yaml
  #         kubectl apply -f k8s/deployment.yaml
